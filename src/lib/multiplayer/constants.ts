/** Maximum human players at a multiplayer table (including host). */
export const MAX_PLAYERS = 6;

/** Minimum players to auto-start a public match. */
export const PUBLIC_MATCH_MIN_PLAYERS = 2;

/** Seconds to wait before starting public match with min players. */
export const PUBLIC_MATCH_START_DELAY_MS = 15_000;
